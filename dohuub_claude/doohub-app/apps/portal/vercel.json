{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install --include=optional --no-package-lock",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
